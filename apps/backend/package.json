{
    "name": "backend",
    "version": "1.0.0",
    "private": "true",
    "scripts": {
        "dev": "tsc-watch --onSuccess \"node dist/index.js\"",
        "start": "node dist/index",
        "build": "tsc -p ."
    },
    "dependencies": {
        "socket.io": "^4.8.1",
        "typescript": "^5.6.3"
    },
    "devDependencies": {
        "tsc-watch": "^6.2.1"
    }
}
